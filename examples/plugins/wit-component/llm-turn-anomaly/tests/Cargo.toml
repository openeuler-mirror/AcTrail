[package]
name = "actrail-llm-turn-anomaly-tests"
version = "0.1.0"
edition = "2024"
publish = false

[dependencies]

[dev-dependencies]
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"

[workspace]
