{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "actrail.llm-turn-anomaly.context-growth.payload.v1",
  "type": "object",
  "required": [
    "root_process_id",
    "display_name",
    "profile_name",
    "growth_ratio_per_mille",
    "minimum_baseline_bytes",
    "minimum_growth_bytes",
    "window_size",
    "minimum_samples",
    "findings",
    "truncated_count"
  ],
  "properties": {
    "root_container_id": { "type": ["string", "null"] },
    "root_process_id": { "type": "string", "minLength": 1 },
    "display_name": { "type": "string", "minLength": 1 },
    "profile_name": { "type": "string", "minLength": 1 },
    "growth_ratio_per_mille": { "type": "integer", "minimum": 1001 },
    "minimum_baseline_bytes": { "type": "integer", "minimum": 1 },
    "minimum_growth_bytes": { "type": "integer", "minimum": 1 },
    "window_size": { "type": "integer", "minimum": 1, "maximum": 64 },
    "minimum_samples": { "type": "integer", "minimum": 1, "maximum": 64 },
    "findings": {
      "type": "array",
      "minItems": 1,
      "maxItems": 4096,
      "items": {
        "type": "object",
        "required": [
          "action_id",
          "call_action_id",
          "process_id",
          "model",
          "observed_bytes",
          "baseline_median_bytes",
          "observed_ratio_per_mille",
          "started_at_ms"
        ],
        "properties": {
          "action_id": { "type": "string", "minLength": 1 },
          "call_action_id": { "type": "string", "minLength": 1 },
          "process_id": { "type": "string", "minLength": 1 },
          "model": { "type": ["string", "null"] },
          "observed_bytes": { "type": "integer", "minimum": 0 },
          "baseline_median_bytes": { "type": "integer", "minimum": 0 },
          "observed_ratio_per_mille": { "type": "integer", "minimum": 0 },
          "started_at_ms": { "type": "integer", "minimum": 0 }
        },
        "additionalProperties": false
      }
    },
    "truncated_count": { "type": "integer", "minimum": 0 }
  },
  "additionalProperties": false
}
