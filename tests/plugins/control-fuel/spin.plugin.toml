[general]
id = "wasm.file-graylist-spin"
api_version = "actrail.plugin.v1"
role = "control-decider"
runtime = "wasm"

[host]
capabilities = []

[runtime.wasm]
artifact_path = "spin.wat"

[runtime.wasm.resources]
fuel_per_call = 10000

[plugin_config]
format = "toml"
schema_ref = "wasm-file-graylist-spin.v1"
required = true
