[general]
id = "wasm.file-graylist-timeout"
api_version = "actrail.plugin.v1"
role = "control-decider"
runtime = "wasm"

[host]
capabilities = []

[runtime.wasm]
artifact_path = "timeout.wat"

[runtime.wasm.resources]
fuel_per_call = 1000000000000

[plugin_config]
format = "toml"
schema_ref = "wasm-file-graylist-timeout.v1"
required = true
