[general]
id = "wasm.unsupported-api"
api_version = "actrail.plugin.v0"
role = "observation-consumer"
runtime = "wasm"

[host]
capabilities = []

[runtime.wasm]
artifact_path = "../wasm-observation/count.wat"

[plugin_config]
format = "toml"
schema_ref = "unsupported-api.v1"
required = true
