[general]
id = "wasm.empty-event-families"
api_version = "actrail.plugin.v2"
role = "observation-consumer"
runtime = "wasm"

[host]
capabilities = []

[runtime.wasm]
artifact_path = "../wasm-observation/count.wat"

[role.observation-consumer.subscriptions]
event_families = []

[plugin_config]
format = "toml"
schema_ref = ""
required = false
