[general]
id = "wasm.missing-schema"
api_version = "actrail.plugin.v2"
role = "observation-consumer"
runtime = "wasm"

[host]
capabilities = []

[runtime.wasm]
artifact_path = "../wasm-observation/count.wat"

[plugin_config]
format = "toml"
schema_ref = "missing.schema.json"
required = true
