[general]
id = "wasm.file-access.current-match-get"
api_version = "actrail.plugin.v2"
role = "control-decider"
runtime = "wasm"

[host]
capabilities = ["file-access.current-match-get"]

[runtime.wasm]
artifact_path = "policy-read.wat"

[role.control-decider.resources]
concurrency_limit = 1

[plugin_config]
format = "toml"
schema_ref = ""
required = false
