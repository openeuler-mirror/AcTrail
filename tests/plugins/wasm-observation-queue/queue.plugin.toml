[general]
id = "wasm.observation-queue"
api_version = "actrail.plugin.v1"
role = "observation-consumer"
runtime = "wasm"

[host]
capabilities = []

[runtime.wasm]
artifact_path = "../wasm-observation/count.wat"

[role.observation-consumer.resources]
queue_capacity = 3

[plugin_config]
format = "toml"
schema_ref = "queue-config.v1"
required = true
