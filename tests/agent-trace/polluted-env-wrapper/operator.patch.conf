control_socket_path = /tmp/actrail-agent-polluted-env-wrapper.sock
control_pid_file = /tmp/actrail-agent-polluted-env-wrapper.pid
control_log_path = /tmp/actrail-agent-polluted-env-wrapper.log
storage_sqlite_path = /tmp/actrail-agent-polluted-env-wrapper.sqlite
web_listen_addr = 127.0.0.1:18121
export_directory = /tmp/actrail-agent-polluted-env-wrapper-export
export_otel_jsonl_path = /tmp/actrail-agent-polluted-env-wrapper-live-spans.otlp.jsonl
capture_profile_name = agent-polluted-env-wrapper
payload_tls_sync_event_socket_path = /tmp/actrail-agent-polluted-env-wrapper-sync.sock
agent_invocation_commands = ["polluted-env-wrapper"]
enforcement_rules_path = /tmp/actrail-agent-polluted-env-wrapper-enforcement.conf
