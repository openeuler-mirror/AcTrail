build_dir = target/agent-trace/gnutls-nss-llm
daemon_ready_timeout_seconds = 10
daemon_stop_timeout_seconds = 5
launch_timeout_seconds = 30
drain_attempts = 40
drain_sleep_seconds = 0.5
payload_head = 120
pre_payload_sleep_ms = 100
post_payload_sleep_ms = 250
model = actrail-legacy-tls-test
prompt = Locate an LLM call over a legacy TLS provider.
response_text = ACTRAIL_GNUTLS_NSS_LLM_OK
