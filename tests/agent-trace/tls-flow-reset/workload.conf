build_dir = target/agent-trace/tls-flow-reset
daemon_ready_timeout_seconds = 10
daemon_stop_timeout_seconds = 5
launch_timeout_seconds = 30
drain_attempts = 30
drain_sleep_seconds = 0.5
payload_head = 120
post_payload_sleep_ms = 200
request = ACTRAIL_TLS_FLOW_RESET_REQUEST
text_marker = ACTRAIL_TLS_FLOW_RESET_TEXT_RESPONSE
