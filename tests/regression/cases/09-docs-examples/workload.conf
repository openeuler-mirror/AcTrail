# Regression controls for docs/examples transfer checks.

control_timeout_seconds = 10
daemon_ready_timeout_seconds = 10
daemon_stop_timeout_seconds = 5
drain_attempts = 25
drain_sleep_seconds = 0.2

quick_start_workload_timeout_seconds = 10
quick_start_socket_timeout_seconds = 5
quick_start_child_hold_seconds = 1
quick_start_post_workload_hold_seconds = 1

http2_local_launch_timeout_seconds = 30
external_llm_launch_timeout_seconds = 90

extended_workload_ready_timeout_seconds = 10
extended_resource_sample_sleep_seconds = 1.2
extended_workload_timeout_seconds = 20
extended_verify_live_timeout_seconds = 30
extended_events_head = 180
extended_network_head = 40
extended_payload_head = 20

agent_invocation_total_timeout_seconds = 160
agent_invocation_xiaoo_availability_max_turns = 1
