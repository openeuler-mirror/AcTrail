# openEuler's generic systemd-tmpfiles-setup.service excludes /dev. Materialize
# the bind-mount source explicitly before kata-agent accepts container requests.
[Service]
ExecStartPre=/usr/bin/systemd-tmpfiles --create --prefix=/dev/actrail
