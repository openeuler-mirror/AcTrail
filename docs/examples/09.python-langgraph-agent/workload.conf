prompt = Reply exactly with ACTRAIL_LANGGRAPH_DOCS_OK
expected_output_fragment = ACTRAIL_LANGGRAPH_DOCS_OK
model = deepseek-chat
base_url = https://api.deepseek.com
chat_path = /chat/completions
api_key_env = DEEPSEEK_API_KEY
request_timeout_seconds = 90
daemon_ready_timeout_seconds = 10
launch_timeout_seconds = 180
drain_attempts = 24
drain_sleep_seconds = 0.5
payload_head = 100
otel_output_path = /tmp/actrail-python-langgraph-agent.otlp.json
daemon_stop_timeout_seconds = 5
evidence_text_max_chars = 1200
