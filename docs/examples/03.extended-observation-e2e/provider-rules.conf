# Provider classification rules used by the AcTrail extended observation E2E.
# Format: rule = provider|field|equals|confidence_millis|rationale
rule = actrail-local-tcp|transport|tcp|950|local TCP transport evidence
