export const TREE_NODE_TYPES = Object.freeze({
  agent: 'agent',
  action: 'action',
  evidence: 'evidence',
});

export const GRAPH_LANES = Object.freeze({
  agent: 'Agent',
  actions: 'Actions',
  details: 'Events',
});

export const UI_LIMITS = Object.freeze({
  inlineAttributeCount: 6,
});
