[package]
name = "trace_runtime"
version.workspace = true
edition = "2024"
license = "MulanPSL-2.0"

[dependencies]
collector_capability = { path = "../../contracts/collector/capability" }
config_core = { path = "../config" }
model_core = { path = "../model" }
