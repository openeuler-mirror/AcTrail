[package]
name = "linux_platform"
version.workspace = true
edition = "2024"
license = "MulanPSL-2.0"

[dependencies]
config_core = { path = "../config" }
libc = "0.2"
linux_cgroup = { path = "../linux_cgroup" }
model_core = { path = "../model" }

[dev-dependencies]
tempfile = "3"
