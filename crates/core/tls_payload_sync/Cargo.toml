[package]
name = "tls_payload_sync"
version.workspace = true
edition = "2024"
license = "MulanPSL-2.0"

[dependencies]
libc = "0.2"
tls_payload_core = { path = "../../tools/tls_payload_core" }
tls_probe_point_finder = { path = "../../tools/tls_probe_point_finder" }
