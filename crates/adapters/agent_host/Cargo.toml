[package]
name = "agent_host"
version.workspace = true
edition = "2024"
license = "MulanPSL-2.0"

[dependencies]
agent_lifecycle_contract = { path = "../../contracts/agent_lifecycle" }
model_core = { path = "../../core/model" }
serde = { version = "1", features = ["derive"] }
toml = "0.8"
