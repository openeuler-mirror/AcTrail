# Agent lifecycle facts and storage contracts.
[package]
name = "agent_lifecycle_contract"
version.workspace = true
edition = "2024"
license = "MulanPSL-2.0"

[dependencies]
model_core = { path = "../../core/model" }
